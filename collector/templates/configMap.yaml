kind: ConfigMap
apiVersion: v1
metadata:
  name: otel-config
data:
  config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

    exporters:
      none:

    service:
      pipelines:
        logs:
          receivers: [otlp]
        traces:
          receivers: [otlp]
        metrics:
          receivers: [otlp]